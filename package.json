{
  "name": "nivi-passports-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx --yes netlify-cli dev",
    "deploy": "npx --yes netlify-cli deploy --prod --build"
  },
  "devDependencies": {
    "@netlify/functions": "^4.2.1"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.14.4",
    "@grpc/proto-loader": "^0.8.1"
  }
}
